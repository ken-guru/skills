# Antigravity Tool Container — extends the shared base image. The Antigravity
# CLI (`agy`) itself installs at container-create time in post-create.sh
# (idempotent, checked by binary path), matching the existing pattern.
FROM {{BASE_IMAGE_TAG}}
