# Generated by setup-devcontainer. One service per selected Tool Container —
# see SKILL.md's compose-assembly step. Every service bind-mounts the same
# workspace root (so .git stays visible to every Tool Container) and can run
# concurrently: open each tool's own devcontainer.json in a separate VS Code
# window (File > New Window + Reopen in Container).
services:
{{COMPOSE_SERVICES}}

volumes:
{{COMPOSE_VOLUMES}}
