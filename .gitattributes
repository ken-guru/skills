# The generated vendor bundle contains upstream multiline string literals whose
# trailing spaces are data, not formatting defects.
skills/presentation/generate-images/scripts/generate-images.js whitespace=-trailing-space
