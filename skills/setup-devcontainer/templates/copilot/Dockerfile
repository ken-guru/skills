# Copilot Tool Container — extends the shared base image. The GitHub Copilot
# CLI itself installs at container-create time in post-create.sh (idempotent,
# checked by binary path), matching the existing pattern for the other
# optional tools.
FROM {{BASE_IMAGE_TAG}}
